import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "News Portal",
  description: "News Portal adalah sebuah platform daring yang menyediakan berita dan informasi terkini dari berbagai bidang seperti politik, ekonomi, hiburan, dan olahraga.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
